<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.projectbelatrix.quejapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo5"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.TabsActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.FormularioActivity" />
        <activity android:name=".Activities.UsuarioPerfilActivity" />
        <activity android:name=".Activities.GetFormDataActivity"></activity>
    </application>

</manifest>